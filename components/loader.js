const Loader = () => {
    return   <p style={{
        margin: '0 auto',
        textAlign: 'center',
        fontSize: 14,
        position: 'fixed',
        top: '50%',
        left: '50%',
        transform: "translate(-30px, -10px)"
    }}>Loading ...</p>
}

export default Loader;